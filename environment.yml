name: surrogate-ferment
channels:
  - conda-forge
  - pytorch
  - defaults
dependencies:
  - python=3.10
  - numpy
  - scipy
  - pandas
  - matplotlib
  - seaborn
  - pip
  - pip:
      - torch>=2.0.0
      - gymnasium>=0.28.0
      - stable-baselines3>=2.0.0
      - pytest>=7.0.0
      - scikit-learn
      - wandb
      - tensorboard
      - optuna
